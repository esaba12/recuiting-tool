transcripts/*
jds/*
!transcripts/.gitkeep
!jds/.gitkeep

# credentials & personal context — never commit
context.md
CLAUDE.local.md
.env
.env.local
.env*.local
*.local.md

# local one-off / verification scripts
app/.verify-*

node_modules/
.worktrees/
.claude/worktrees/
.vercel

# graphify knowledge-graph output — regenerate with `graphify . --no-viz --code-only`
graphify-out/
